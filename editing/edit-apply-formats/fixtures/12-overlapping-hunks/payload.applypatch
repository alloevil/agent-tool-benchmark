*** Begin Patch
*** Update File: file.py
@@
     return a - b
 
 
-def multiply(a, b):
-    return a * b
+def multiply(a, b):
+    product = a * b
+    return product
 
 
 def divide(a, b):
@@
     return a * b
 
 
-def divide(a, b):
-    # TODO: handle divide by zero
-    return a / b
+def divide(a, b):
+    if b == 0:
+        raise ZeroDivisionError("division by zero is not allowed")
+    return a / b
 
 
 def apply_op(op, a, b):
*** End Patch
